# Obsidian_StatblockBridge
A companion utility or a set of custom CSS/YAML templates that automatically ingest legacy monster data and format it into the new 2024 layout, complete with the new actions and traits.
